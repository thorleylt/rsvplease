// This is a placeholder method emulating the process of storing the invite in our DB
const storeRsvp = (inviteId: string, invitee: string) => {
  return {
    inviteId,
    hostEmail: "lewis@reflr.io",
  };
};

export default storeRsvp;
